// Sample arrays
const numbers = [1, 2, 3, 4, 5, 6];
const words = ['apple', 'banana', 'cat', 'dog'];
const letters = ['a', 'b', 'c', 'd'];

// 1. Use filter() to return all numbers from the 'numbers' array that are greater than 3.
function filterNumbers(arr) {
  // Use filter to return numbers greater than 3
}

// 2. Use the unshift() method to add the number 0 to the beginning of the 'numbers' array and return the updated array.
function addElementToStart(arr) {
  // Use unshift to add 0 at the beginning
}

// 3. Using the reduce() method, find the longest word in the 'words' array and return it.
function findLongestWord(arr) {
  // Use reduce to find the longest word
}

// 4. Use the splice() method to remove the second and third elements from the 'letters' array and return the updated array.
function removeElements(arr) {
  // Use splice to remove the second and third elements
}

// 5. Use the indexOf() method to find the index of the word 'cat' in the 'words' array and return the index.
function findWordIndex(arr) {
  // Use indexOf to find the index of 'cat'
}
